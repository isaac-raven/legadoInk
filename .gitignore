# =============================================================================
# 阅读 Sigma —— 开源清理版
# 本文件在原工程基础上扩充了「第三方内容 / 个人隐私 / 签名密钥」相关忽略规则。
# 规则按用途分组，新增文件前请确认不会被下面的规则误伤。
# =============================================================================

# -----------------------------------------------------------------------------
# 1. 签名密钥与凭据 —— 绝对禁止入库
# -----------------------------------------------------------------------------
*.jks
*.keystore
*.p12
*.pfx
*.pem
*.key
app/key.jks
app/legado.jks
# GitHub Actions 签名所需的四个变量，由 CI Secrets 注入，不得写入仓库
RELEASE_KEY_ALIAS
RELEASE_KEY_PASSWORD
RELEASE_STORE_PASSWORD
RELEASE_KEY_STORE

# -----------------------------------------------------------------------------
# 2. 本地环境配置 —— 含本机绝对路径，禁止入库
# -----------------------------------------------------------------------------
local.properties
.gradle/
.kotlin/
.cxx/
.externalNativeBuild/
.idea/
.vscode/
*.iml
/captures
.DS_Store

# -----------------------------------------------------------------------------
# 3. 构建产物
# -----------------------------------------------------------------------------
build/
/app/app
/app/google
/app/gradle.properties
/release
/tmp
*.apk
*.aab
*.ap_
*.idsig
output-metadata.json
baselineProfiles/
*.hprof

# -----------------------------------------------------------------------------
# 4. 第三方字体 —— 不随仓库分发，由 scripts/download-fonts.sh 按需获取
#    注意：只忽略「阅读字体」目录，不要全局忽略 *.ttf，
#    因为 app/src/main/assets/font/number.ttf 等是工程自带的合法字体。
# -----------------------------------------------------------------------------
app/src/main/assets/fonts/*.ttf
app/src/main/assets/fonts/*.otf
app/src/main/assets/fonts/*.ttc

# -----------------------------------------------------------------------------
# 5. 本地书籍、缓存与导出数据 —— 可能包含受版权保护的内容或个人阅读记录
#
#    ⚠️ 注意：以下目录规则**必须**用前导斜杠锚定到仓库根目录。
#    源码中存在 app/src/main/java/.../bookshelf/style1/books/ 与
#    .../ui/book/cache/ 等目录，若写成无锚定的 books/ 、cache/ 会把
#    这些 Kotlin 源码一并排除，直接导致编译失败。
# -----------------------------------------------------------------------------
/books/
/local-books/
/downloads/
/cache/
/backup/
/webdav-cache/
*.epub
*.mobi
*.azw3
*.azw
*.pdf
*.cbz
*.cbr
# 备份导出（含书架、阅读进度、书签等个人数据）
*.legado

# -----------------------------------------------------------------------------
# 6. 前端依赖
# -----------------------------------------------------------------------------
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml
